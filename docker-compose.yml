services:
  webspeak:
    image: ${WEBSPEAK_IMAGE:-ghcr.io/echosixhiya/webspeak:latest}
    pull_policy: missing
    restart: unless-stopped
    ports:
      - "${WEBSPEAK_PORT:-3040}:3040"
      - "40000-40099:40000-40099/udp"
    volumes:
      - webspeak-data:/data

volumes:
  webspeak-data:
