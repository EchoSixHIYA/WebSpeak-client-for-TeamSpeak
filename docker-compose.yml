services:
  webspeak:
    build:
      context: .
      dockerfile: Dockerfile
    image: webspeak:local
    restart: unless-stopped
    ports:
      - "${WEBSPEAK_PORT:-3040}:3040"
    volumes:
      - webspeak-data:/data

volumes:
  webspeak-data:
